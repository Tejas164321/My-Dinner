rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // WARNING: These rules are insecure and should not be used in production.
      // They allow public access for development purposes.
      allow read, write: if true;
    }
  }
}
